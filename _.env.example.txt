# Copy to .env (or set these in Portainer's stack "Environment variables" UI).
# Do NOT commit real values.

# GitHub Personal Access Token (classic or fine-grained with repo scope).
# Used by both gh and git for push/pull over HTTPS.
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Git commit identity
GIT_AUTHOR_NAME=Sean Cullinan
GIT_AUTHOR_EMAIL=you@example.com

# --- Optional: auto-start Remote Control in tmux when the container starts ---
# Only works AFTER you've done the one-time `claude` -> `/login` to claude.ai.
# 1 = enable, 0 = disable (default)
AUTO_REMOTE_CONTROL=0
# Which directory the auto-started session opens in
RC_PROJECT_DIR=/workspace
# Session title shown at claude.ai/code and in the mobile app
RC_SESSION_NAME=claude-dev
# How server mode spawns sessions: same-dir | worktree | session
# worktree = each session gets its own git worktree (best for multiple repos)
RC_SPAWN_MODE=worktree
# Seconds the supervisor waits before relaunching a dead session
RC_RESTART_DELAY=10
