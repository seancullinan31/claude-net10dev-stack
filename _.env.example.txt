# Copy to .env (or set these in Portainer's stack "Environment variables" UI).
# Do NOT commit real values.

# GitHub Personal Access Token (classic or fine-grained with repo scope).
# Used by both gh and git for push/pull over HTTPS.
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Git commit identity
GIT_AUTHOR_NAME=Sean Cullinan
GIT_AUTHOR_EMAIL=you@example.com

# --- Optional: auto-start one named Remote Control session per repo ---
# Only works AFTER you've done the one-time `claude` -> `/login` to claude.ai.
# 1 = enable, 0 = disable (default)
AUTO_REMOTE_CONTROL=0
# Root directory scanned for git repos (each becomes its own named session)
WORKSPACE=/workspace
# Seconds the supervisor waits before relaunching a dead session
RC_RESTART_DELAY=10
# Seconds between rescans for newly-added repos
RC_RESCAN_INTERVAL=60
# Permission mode each spawned session starts in:
#   default | acceptEdits | plan | auto | bypassPermissions
# NOTE: bypassPermissions is BLOCKED when running as root (this container is root),
# so it will crash-loop. Use 'auto' (classifier auto-approves routine commands,
# still asks on risky ones) or 'acceptEdits'. Use 'default' for normal prompting.
RC_PERMISSION_MODE=auto
